@{
    ViewData["Title"] = "Home Page";

    var featuredMovies = ViewData["FeaturedMovies"] as List<ASPNET_Core_MVC.Models.Movie> ?? new List<ASPNET_Core_MVC.Models.Movie>();
    var recentMovies = ViewData["RecentMovies"] as List<ASPNET_Core_MVC.Models.Movie> ?? new List<ASPNET_Core_MVC.Models.Movie>();
    var featuredTvSeries = ViewData["FeaturedTvSeries"] as List<ASPNET_Core_MVC.Models.TvSeries> ?? new List<ASPNET_Core_MVC.Models.TvSeries>();
    var recentTvSeries = ViewData["RecentTvSeries"] as List<ASPNET_Core_MVC.Models.TvSeries> ?? new List<ASPNET_Core_MVC.Models.TvSeries>();
}

<div class="container">
    <h2 class="section-title">Featured Movies</h2>
    <div class="row">
        @if (featuredMovies.Any())
        {
            @foreach (var movie in featuredMovies)
            {
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <img src="@(string.IsNullOrEmpty(movie.ImageUrl) ? "/images/default-movie.jpg" : movie.ImageUrl)" class="card-img-top" alt="@movie.Title">
                        <div class="card-body">
                            <h5 class="card-title">@movie.Title</h5>
                            <p class="card-text">@movie.ReleaseYear</p>
                            <p class="card-text">@movie.Description</p>
                            <a asp-controller="Movies" asp-action="Details" asp-route-id="@movie.Id" class="btn btn-primary">View Details</a>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <p>No featured movies available.</p>
            </div>
        }
    </div>

    <h2 class="section-title">Recent Movies</h2>
    <div class="row">
        @if (recentMovies.Any())
        {
            @foreach (var movie in recentMovies)
            {
                <div class="col-md-2 mb-4">
                    <div class="card h-100">
                        <img src="@(string.IsNullOrEmpty(movie.ImageUrl) ? "/images/default-movie.jpg" : movie.ImageUrl)" class="card-img-top" alt="@movie.Title">
                        <div class="card-body">
                            <h5 class="card-title">@movie.Title</h5>
                            <p class="card-text">@movie.ReleaseYear</p>
                            <a asp-controller="Movies" asp-action="Details" asp-route-id="@movie.Id" class="btn btn-sm btn-primary">Details</a>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <p>No recent movies available.</p>
            </div>
        }
    </div>

    <h2 class="section-title">Featured TV Series</h2>
    <div class="row">
        @if (featuredTvSeries.Any())
        {
            @foreach (var series in featuredTvSeries)
            {
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <img src="@(string.IsNullOrEmpty(series.ImageUrl) ? "/images/default-series.jpg" : series.ImageUrl)" class="card-img-top" alt="@series.Title">
                        <div class="card-body">
                            <h5 class="card-title">@series.Title</h5>
                            <p class="card-text">@series.StartYear</p>
                            <p class="card-text">@series.Description</p>
                            <a asp-controller="TvSeries" asp-action="Details" asp-route-id="@series.Id" class="btn btn-primary">View Details</a>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <p>No featured TV series available.</p>
            </div>
        }
    </div>

    <h2 class="section-title">Recent TV Series</h2>
    <div class="row">
        @if (recentTvSeries.Any())
        {
            @foreach (var series in recentTvSeries)
            {
                <div class="col-md-2 mb-4">
                    <div class="card h-100">
                        <img src="@(string.IsNullOrEmpty(series.ImageUrl) ? "/images/default-series.jpg" : series.ImageUrl)" class="card-img-top" alt="@series.Title">
                        <div class="card-body">
                            <h5 class="card-title">@series.Title</h5>
                            <p class="card-text">@series.StartYear</p>
                            <a asp-controller="TvSeries" asp-action="Details" asp-route-id="@series.Id" class="btn btn-sm btn-primary">Details</a>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12">
                <p>No recent TV series available.</p>
            </div>
        }
    </div>
</div>